For PressureBlocks:

public float MaxPressure = 0f;
public float MinPressure = 0f;
public boolean ConnectsPressureSystems = false;

For PressureBuilds

public Seq<Building> PressureSystem = new Seq<Building>();

For PressureGenerators

public float MaxGeneratedPressure = 0f;
public float MinGeneratedPressure = 0f;

For PressureGeneratorBuilds

public boolean ProducingMax = true;